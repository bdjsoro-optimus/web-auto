```
---
의뢰인: {이름}
분석 일시: {YYYY-MM-DD}
진단 보고서: AI자동화_진단보고서_{이름}_{날짜}.md
설계도: AI자동화_설계도_{이름}_{날짜}.md
설계도 버전: v1
버전: v1
---
```

# 🛠️ 개발 지침서

> **개발 AI 대상** - "그 설계대로 빌드할 때 이런 기술 스펙으로" (HOW)

---

## 1. 아키텍처 구체

### 기술 스택 (구체 버전)

| 영역 | 스택 + 버전 | 출처 / 검색일 |
|------|------------|---------------|
| 프론트엔드 | React 18.x | 설계 §3 + 검색일 YYYY-MM-DD |
| 백엔드 | Node.js 20 LTS + Express 4.18.x | 설계 §3 + 검색일 YYYY-MM-DD |
| DB | PostgreSQL 16 | 설계 §4 + 검색일 YYYY-MM-DD |

### 핵심 라이브러리

| 라이브러리 | 버전 | 용도 | 출처 |
|-----------|------|------|------|
| {lib} | {ver} | {용도} | 설계 § N + 검색일 YYYY-MM-DD |

⚠️ deprecation 검증 완료 (검색일 YYYY-MM-DD 기준 안정 버전)

---

## 2. 데이터 모델 구체

### 스키마 (실행 가능 코드)

```sql
-- 선택된 DB에 맞춰 (PostgreSQL / MongoDB / Mongoose 등)
CREATE TABLE {엔티티1} (
  id UUID PRIMARY KEY,
  ...
);
```

### 인덱스 (성능)
- {엔티티1}.{필드}: B-tree (← 설계 § N "{이유}")
- ...

### 출처 매핑
- {엔티티1} ← 설계 § 4 "{엔티티 이름}"

---

## 3. 워크플로 구현

### Step 1: {단계명}
- **입력**: {입력 형식}
- **출력**: {출력 형식}
- **사용 라이브러리**: {라이브러리}
- **에러 처리**: {예외 케이스 + 처리 방식}
- **코드 스니펫** (실행 가능):
```javascript
// 의미 있는 변수명 + 주석
async function {함수명}({매개변수}) {
  try {
    // ...
  } catch (err) {
    // 에러 처리
  }
}
```

### Step 2: ...

(설계 §2 기능 P0/P1/P2 순서대로 반복)

---

## 4. API 구현

### 인증 방식
- {방식} (JWT / OAuth 2.0 / API Key 등) (← 설계 § 6 / 검색일 YYYY-MM-DD)
- 토큰 만료: {기간}
- 갱신: {방식}

### 엔드포인트별 구현 디테일

#### POST /api/{...}
- **요청 헤더**: `Authorization: Bearer ...`
- **요청 본문**: `{...}`
- **응답 (성공 200)**: `{...}`
- **에러 코드**:
  - 400: {언제}
  - 401: {언제}
  - 500: {언제}
- **Rate limit**: N requests / minute
- **코드 스니펫**:
```javascript
// ...
```

(설계 § 6 엔드포인트 모두 반복)

---

## 5. 배포 및 운영

### 권장 배포 플랫폼
- **{플랫폼}**: {이유} (← 설계 § 3 + Q1 호스팅 + 검색일 YYYY-MM-DD)
- 비용: {월 예상} (검색일 YYYY-MM-DD 기준)

### 환경 변수

⚠️ 실제 키·시크릿은 `.env` 파일에. 코드·git에 절대 포함 X.

```bash
# .env.example
DATABASE_URL=postgresql://user:pass@host:5432/db
JWT_SECRET={최소 32자 무작위 문자열}
{API_KEY}={발급받은_키}
```

### CI/CD 파이프라인
- **도구**: {GitHub Actions / CircleCI / etc.} (검색일 YYYY-MM-DD)
- **단계**:
  1. push → 테스트 자동 실행
  2. main 브랜치 → 스테이징 배포
  3. tag 릴리스 → 프로덕션 배포

### 모니터링·로깅
- **로그**: {도구} (예: Loggly / CloudWatch)
- **에러 추적**: {도구} (예: Sentry)
- **성능 지표**: {도구}

---

## 6. 비용 분석

### AI API 사용료 (월 예상, 검색일 YYYY-MM-DD)

| API | 무료 티어 | 유료 시 추정 | 출처 |
|-----|----------|-------------|------|
| {API1} | {제한} | {월 $N} | {링크} |
| {API2} | ... | ... | ... |

### 호스팅 비용 (월 예상)
- {플랫폼}: $N/월 (검색일 YYYY-MM-DD)

### 총 운영 비용 추정 (월)
- 무료 티어 활용: $0~$N
- 일반 사용: $N~$M
- 사용자 증가 시 (1k MAU 가정): $N

---

## 7. 개발 로드맵

### Phase 1 (MVP) — {기간 예: 1~2주}
- {핵심 기능 1} (설계 § 2 P0)
- {핵심 기능 2} (설계 § 2 P0)

### Phase 2 — {기간 예: 3~4주}
- {기능 3} (설계 § 2 P1)
- {기능 4} (설계 § 2 P1)
- 최적화

### Phase 3 — {기간 예: 5~8주}
- {기능 5} (설계 § 2 P2)
- 고도화·확장

---

## 8. 참고 자료

### 공식 문서 (검색일 YYYY-MM-DD)
- [{스택1} 공식 문서]({링크})
- [{스택2} 공식 문서]({링크})
- [{API1} 공식 문서]({링크})

### 가이드·레퍼런스
- {링크}: {용도}

---

> **생성 정보**
> - 생성일: YYYY-MM-DD HH:MM (자동)
> - AI 엔진: Claude
> - 검색일: YYYY-MM-DD (WebSearch 결과 기반)
> - 입력: AI자동화_진단보고서_{이름}_{날짜}.md + AI자동화_설계도_{이름}_{날짜}.md

═══════════════════════
[양식 메타]
- 8섹션 + 푸터
- 핵심 원칙: 코드 실행 가능 / inline 설계 § N 출처 / WebSearch 검색일 / HOW 중심
- 헤더 이모지: 🛠️ (메인 타이틀)
- 환경 변수 ⚠️ 보안 권고 명시
- frontmatter: 의뢰인 / 분석 일시 / 진단 보고서 / 설계도 / 설계도 버전 / 버전
- 저장 위치: notes/양식/개발지침서_양식.md
═══════════════════════
